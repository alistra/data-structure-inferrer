Different effects depending on operation usage (e.g. better structures for more inserts than changes)

Different effects depending on the DS being static or dynamic.

Different effects depending on the existance of user input (dynamic number of operations).

DS to add:
	unionfind
	brodaqueue/fiboheap/binomheap
	array

OPS to add:
	union/intersection/difference/symdifference
	decreaseKey
